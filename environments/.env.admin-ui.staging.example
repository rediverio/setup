# =============================================================================
# Admin UI Configuration - Staging
# =============================================================================
# Platform Administration Console configuration
#
# Usage: Copy to .env.admin-ui.staging
#   cp .env.admin-ui.staging.example .env.admin-ui.staging
#
# =============================================================================

VERSION=staging-latest

# -----------------------------------------------------------------------------
# Build-time Variables (NEXT_PUBLIC_*)
# These are passed as build args to Docker
# -----------------------------------------------------------------------------

# Admin API URL - where the platform admin API is running
# This is baked into the client bundle at build time
NEXT_PUBLIC_API_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Runtime Variables
# -----------------------------------------------------------------------------

# Port mapping (internal is always 3000)
ADMIN_UI_PORT=3001
