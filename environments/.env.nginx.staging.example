# =============================================================================
# Nginx Configuration - Staging
# =============================================================================
# Nginx reverse proxy configuration for staging environment.
#
# Usage: Copy to .env.nginx.staging
#   cp environments/.env.nginx.staging.example .env.nginx.staging
#
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Server hostname for nginx
# - localhost: for local development
# - staging.example.com: for staging server with domain
# - _: to match any hostname (not recommended)
NGINX_HOST=localhost
