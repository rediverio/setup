# =============================================================================
# Nginx Configuration - Staging
# =============================================================================
# Nginx reverse proxy configuration for staging environment.
#
# Usage: Copy to .env.nginx.staging
#   cp environments/.env.nginx.staging.example .env.nginx.staging
#
# =============================================================================

# -----------------------------------------------------------------------------
# UI Server Configuration
# -----------------------------------------------------------------------------
# UI hostname for nginx (rediver.io / app.rediver.io)
# - localhost: for local development
# - staging.example.com: for staging server with domain
# - _: to match any hostname (not recommended)
NGINX_HOST=localhost

# -----------------------------------------------------------------------------
# API Server Configuration
# -----------------------------------------------------------------------------
# API hostname for nginx (api.rediver.io)
# - api.localhost: for local development
# - api.staging.example.com: for staging server with domain
API_HOST=api.localhost

# -----------------------------------------------------------------------------
# Admin UI Server Configuration
# -----------------------------------------------------------------------------
# Admin UI hostname for nginx (admin.rediver.io)
# - admin.localhost: for local development
# - admin.staging.example.com: for staging server with domain
ADMIN_HOST=admin.localhost
