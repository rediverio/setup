# =============================================================================
# Nginx Configuration - Production
# =============================================================================
# Nginx reverse proxy configuration for production environment.
#
# Usage: Copy to .env.nginx.prod
#   cp environments/.env.nginx.prod.example .env.nginx.prod
#
# IMPORTANT: Both NGINX_HOST and API_HOST are REQUIRED for production!
#
# =============================================================================

# -----------------------------------------------------------------------------
# UI Server Configuration
# -----------------------------------------------------------------------------
# UI hostname for nginx (REQUIRED)
# Must match your actual domain name
# Example: rediver.io, app.rediver.io
NGINX_HOST=<CHANGE_ME_YOUR_DOMAIN>

# -----------------------------------------------------------------------------
# API Server Configuration
# -----------------------------------------------------------------------------
# API hostname for nginx (REQUIRED)
# Must match your actual API domain name
# Example: api.rediver.io
API_HOST=<CHANGE_ME_YOUR_API_DOMAIN>
