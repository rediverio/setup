# =============================================================================
# Database Configuration - Production
# =============================================================================
# Database and Redis credentials for production environment.
# This file contains sensitive credentials - keep it secure!
#
# IMPORTANT:
#   - Use strong, unique passwords
#   - Never commit actual credentials to version control
#   - Restrict access to this file (chmod 600)
#
# Usage: Copy to .env.db.prod
#   cp .env.db.prod.example .env.db.prod
#
# Generate passwords:
#   openssl rand -base64 32
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------
DB_USER=rediver
DB_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>
DB_NAME=rediver

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
# REQUIRED for production - generate a strong password
REDIS_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>
