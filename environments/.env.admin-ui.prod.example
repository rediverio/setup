# =============================================================================
# Admin UI Configuration - Production
# =============================================================================
# Platform Administration Console configuration
#
# Usage: Copy to .env.admin-ui.prod
#   cp .env.admin-ui.prod.example .env.admin-ui.prod
#
# =============================================================================

VERSION=latest

# -----------------------------------------------------------------------------
# Build-time Variables (NEXT_PUBLIC_*)
# These are passed as build args to Docker
# -----------------------------------------------------------------------------

# Admin API URL - where the platform admin API is running
# This should be the public URL that the browser can reach
# Example: https://api.admin.rediver.io or https://admin-api.rediver.io
NEXT_PUBLIC_API_URL=<CHANGE_ME>

# -----------------------------------------------------------------------------
# Runtime Variables
# -----------------------------------------------------------------------------

# Port is fixed at 3000 internally, nginx handles external routing
