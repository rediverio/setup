-- =============================================================================
-- Required Seed Data for Rediver Platform
-- =============================================================================
-- This file contains essential data that the system REQUIRES to function.
-- Run this after migrations to initialize the database with required data.
-- =============================================================================
-- Usage: make docker-seed-required
-- =============================================================================

-- Currently, the Rediver platform does not require any pre-seeded data.
-- All data (users, tenants, assets, etc.) is created dynamically by users.

-- If you need to add required system data in the future (e.g., default roles,
-- system configurations, default categories), add them here.

-- Example of required data you might add:
-- INSERT INTO system_settings (key, value) VALUES ('app_version', '1.0.0');

-- =============================================================================
-- END OF REQUIRED SEED DATA
-- =============================================================================
